apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tekton-pipelines

resources:
  - rbac.yaml
  - secrets.yaml
  - pipeline.yaml
  - trigger.yaml
  - ingress.yaml

# Don't include pipelinerun.yaml - it's for manual runs
