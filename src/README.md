# Rust Token Verification Library (Legacy)

This is the Rust HMAC-SHA256 token verification library from ATAuth v1.x.

**Status: Unmaintained.** The gateway now handles all token operations directly via standard OIDC JWTs (ES256). This library is preserved for reference but is not actively developed.

For current usage, see the [gateway/](../gateway/) directory.
